{
	"pluginAlias": "SensiboAC",
	"pluginType": "platform",
	"singular": true,
	"headerDisplay": "Homebridge plugin for Sensibo - Smart AC Control",
	"footerDisplay": "Created by @nitaybz, maintained by volunteers, please consider a ☆ on GitHub if you find it useful!",
	"schema": {
		"type": "object",
		"properties": {
			"auth": {
				"title": "Auth Mode",
				"type": "string",
				"default": "api-key",
				"oneOf": [
					{ "title": "API Key", "enum": ["api-key"] },
					{ "title": "Username and Password", "enum": ["usernamePassword"] }
				],
				"required": true
			},
			"apiKey": {
				"title": "API Key",
				"description": "An API Key can be used instead of username/password, to create a Sensibo API key visit https://home.sensibo.com/me/api",
				"type": "string",
				"placeholder": "API Key",
				"required": false,
				"condition": {
					"functionBody": "return model.auth === \"api-key\";"
				}
			},
			"username": {
				"title": "Username",
				"description": "Your Sensibo account username/email",
				"type": "string",
				"required": false,				
				"placeholder": "Username",
				"condition": {
					"functionBody": "return model.auth === \"usernamePassword\";"
				}
			},
			"password": {
				"title": "Password",
				"description": "Your Sensibo account password",
				"type": "string",
				"placeholder": "Password",
				"required": false,
				"condition": {
					"functionBody": "return model.auth === \"usernamePassword\";"
				}
			},
			"modesToExclude": {
				"title": "Air Conditioning Modes to Exclude",
				"description": "Select the AC modes you want <strong>removed</strong> from your AC Accessories in the Home app (leave empty to keep all available modes)",
				"type": "array",
				"items": {
					"type": "string",
					"enum": [
						"AUTO",
						"COOL",
						"DRY",
						"FAN",
						"HEAT"
					]
				}
			},
			"allowRepeatedCommands": {
				"title": "Allow Repeated Commands",
				"description": "Allow the plugin to send the same state command again",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"carbonDioxideAlertThreshold": {
				"title": "Carbon Dioxide (CO2) Alert Threshold",
				"description": "Value, in PPM, over which the Apple Home app will alert you to high CO2 readings.  Default is 1500. Requires the Carbon Dioxide Sensor be enabled and a compatible device",
				"type": "integer",
				"minimum": 0,
				"placeholder": 1500,
				"required": false,
				"condition": {
					"functionBody": "return model.disableCarbonDioxide === false;"
				}
			},
			"disableAirQuality": {
				"title": "Disable Air Quality Sensor",
				"description": "Remove Air Quality and TVOC readings",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"disableCarbonDioxide": {
				"title": "Disable Carbon Dioxide Sensor",
				"description": "Remove carbon dioxide (CO2) readings",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"disableDry": {
				"title": "Disable Dry Accessory",
				"description": "<span class=\"text-danger\"><strong>*Deprecated*</strong></span> Remove dehumidifier (dry) mode from ACs. Use Modes to Exclude (above) instead",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"disableFan": {
				"title": "Disable Fan Accessory",
				"description": "<span class=\"text-danger\"><strong>*Deprecated*</strong></span> Remove fan mode from ACs. Use Modes to Exclude (above) instead",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"disableHumidity": {
				"title": "Disable Humidity Sensor",
				"description": "Remove Humidity readings. Note: humidity will still show if you have Dry mode enabled",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"externalHumiditySensor": {
				"title": "Enable External Humidity Sensor",
				"description": "Add (additional) standalone Humidity Sensor accessory",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"disableLightSwitch": {
				"title": "Disable Light Switch",
				"description": "Remove light switch",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"disableHorizontalSwing": {
				"title": "Disable Horizontal Swing",
				"description": "Remove horizontal swing control switch",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"disableVerticalSwing": {
				"title": "Disable Vertical Swing",
				"description": "Remove vertical swing (Oscillate) control switch from accessory",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"enableClimateReactSwitch": {
				"title": "Enable Climate React Switch",
				"description": "Add Climate React (Smart mode) switch",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"climateReactSwitchInAccessory": {
                "title": "Climate React Switch in AC Accessory",
                "description": "Show the Climate React (Smart mode) switch in ACs (only works when `enableClimateReactSwitch` is set to true)",
                "type": "boolean",
                "default": false,
                "required": false
            },
            "climateReactAsAutoMode": {
                "title": "Use Climate React as the AUTO mode",
                "description": "Back the HomeKit AUTO mode with Climate React (Smart mode) instead of the AC's native AUTO. In AUTO nothing is sent to the AC - Climate React alone switches it on/off, so the fan stops between cycles. The AUTO temperature range sets the Climate React low/high thresholds. Switching to COOL or OFF disables Climate React. Exclude HEAT from the modes when using this.",
                "type": "boolean",
                "default": false,
                "required": false
            },
            "climateReactAutoLowOffset": {
                "title": "AUTO low threshold offset (°C)",
                "description": "Added to the LOW threshold sent to Climate React only, so HomeKit 23 becomes 23.2 in Climate React. The high threshold is never offset. Default 0.2.",
                "type": "number",
                "default": 0.2,
                "required": false
            },
            "climateReactAutoTargetTemperature": {
                "title": "AUTO: AC setpoint while cooling (°C)",
                "description": "The temperature Climate React commands on the AC when it switches the unit on in AUTO. Independent of the AUTO range: the range decides when the AC runs, this decides how hard it cools. Clamped to what the AC supports. Default 22.",
                "type": "integer",
                "default": 22,
                "required": false
            },
            "climateReactAutoFanLevel": {
                "title": "AUTO: fan level while cooling",
                "description": "The fan level Climate React commands on the AC when it switches the unit on in AUTO. Falls back to the current fan level if the AC doesn't support the chosen one. Default low.",
                "type": "string",
                "default": "low",
                "oneOf": [
                    { "title": "Low", "enum": ["low"] },
                    { "title": "Medium low", "enum": ["medium_low"] },
                    { "title": "Medium", "enum": ["medium"] },
                    { "title": "Medium high", "enum": ["medium_high"] },
                    { "title": "High", "enum": ["high"] },
                    { "title": "Strong", "enum": ["strong"] },
                    { "title": "Auto", "enum": ["auto"] }
                ],
                "required": false
            },
            "climateReactAutoTemperatureStep": {
                "title": "AUTO: temperature range step (°C)",
                "description": "Step for the HomeKit AUTO range. Climate React thresholds are triggers rather than AC setpoints, so they can be finer than the whole degrees the AC accepts - 0.5 allows ranges like 23.5-24.5. Commands sent to the AC itself are still rounded to a whole degree. Celsius only. Default 1.",
                "type": "number",
                "default": 1,
                "oneOf": [
                    { "title": "1.0°", "enum": [1] },
                    { "title": "0.5°", "enum": [0.5] }
                ],
                "required": false
            },
            "climateReactAutoDebounceMs": {
                "title": "Climate React update debounce (ms)",
                "description": "How long to wait after the last change before sending the Climate React update. Coalesces the burst of updates HomeKit sends when dragging the AUTO range, avoiding rate-limit (429) errors. Default 3000.",
                "type": "integer",
                "default": 3000,
                "minimum": 0,
                "required": false
            },
            "enableClimateReactAutoSetup": {
                "title": "Enable Climate React Auto Setup",
                "description": "Auto-update the Climate React (Smart mode) configuration to align whenever the AC state is set or changed",
                "type": "boolean",
                "default": false,
                "required": false
            },
			"enableHistoryStorage": {
				"title": "Enable History Storage",
				"description": "Record temperature & humidity measurements over time, viewable as History in the Eve app",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"enableOccupancySensor": {
				"title": "Enable Occupancy Sensor",
				"description": "Add occupancy sensor to represent the state of someone at home/away",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"enableSyncButton": {
				"title": "Enable Sync Switch",
				"description": "Add a switch to toggle the state of the AC, without sending commands to the unit",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"syncButtonInAccessory": {
				"title": "Sync Button in AC Accessory",
				"description": "Show the AC Sync switch in accessory",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"ignoreHomeKitDevices": {
				"title": "Ignore HomeKit Devices",
				"description": "Automatically ignore, skip or remove HomeKit supported devices",
				"type": "boolean",
				"default": false,
				"required": false
			},
			"devicesToExclude": {
				"title": "Devices to Exclude (Name/ID/Serial)",
				"description": "Add device identifiers (Name, ID from logs or serial from Home app) to exclude them from homebridge",
				"type": "array",
				"items": {
					"type": "string"
				}
			},
			"locationsToInclude": {
				"title": "Device Locations to Include",
				"description": "Add device location IDs or names to include when discovering Sensibo devices (leave empty for all locations)",
				"type": "array",
				"items": {
					"type": "string"
				}
			},
			"debug": {
				"title": "Enable Debug Logs",
				"description": "Enable extra logging for debugging purposes",
				"type": "boolean",
				"default": false,
				"required": false
			}
		},
		"anyOf": [
			{
				"properties": {
					"auth": { "const": "api-key" }
				},
				"required": ["apiKey"]
			},
			{
				"properties": {
					"auth": { "const": "usernamePassword" }
				},
				"required": ["username","password"]
			}
		]
	}
}